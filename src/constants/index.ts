/**
 * Application-wide constants
 * Centralizes magic numbers and configuration values
 */

// Search and Filtering
export const SEARCH_DEBOUNCE_DELAY_MS = 200 // Optimized for faster backend
export const SEARCH_MIN_CHARACTERS = 2 // Minimum characters to trigger search

// Pagination
export const DEFAULT_PAGE_SIZE_TRANSACTIONS = 20
export const DEFAULT_PAGE_SIZE_CATEGORIES = 12

// Validation
export const MAX_AMOUNT_VALUE = 999999999
export const MIN_AMOUNT_VALUE = 0.01
